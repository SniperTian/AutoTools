# AutoTools 验证记录

环境：Windows 11 x64，Python 3.12.14，Tcl/Tk 8.6.15。

## 自动化测试

`python -m unittest discover -s tests -v`：60 项，59 项通过、1 项跳过。

- 真实 Win32 Edit 控件：最小化状态下执行 Enter 后检查实际文本为“测试\r\n”；Ctrl+A 后检查实际选区为全文；窗口保持最小化，前台焦点不变。
- Ctrl+V 调用 WM_PASTE，未发送 Ctrl/V 字母消息；未识别编辑区时拒绝执行并报告原因；命令超时报告失败。这些分支使用替代 API 验证调用和错误处理。
- 完整剪贴板粘贴循环测试位于 tests/native_edit_probe.py，仅在独立窗口站运行，避免修改用户剪贴板。当前环境拒绝创建独立窗口站，因此该项跳过。尚未完成用户记事本版本的实际粘贴验收，不能把消息调用测试等同于记事本实测。
- 原有后台消息顺序、最小化消息接收、停止释放、PID 身份校验、录制过滤、快捷键捕获与按页启用等测试通过。
- 真实 Tk 控件验证托盘默认关闭、勾选图像至少 26px 并随 DPI 放大、说明位于右侧、配置保存、坐标区与按键表格对齐。
- 托盘线程测试使用真实 Win32 隐藏窗口和图标，通知区 API 被替代；用户已反馈上一版托盘功能实机可用。

## 发布包验证

- 已确认旧 ZIP 缺少 _internal/base_library.zip。原因是压缩脚本把全部 .zip 都当成不需要的文件排除。
- 修复为仅源码归档排除已有 ZIP，EXE 归档保留运行库 ZIP，并核对完整文件清单和 CRC。
- tools/verify_release.py 解压实际发布包到新的目录，逐文件比对归档内容，再运行解压后的 AutoTools.exe --startup-check。
- 自检清除 Conda/Python 环境变量、PATH 仅保留 System32，成功启动内嵌 Python 3.12.14，加载 Tk 8.6.15、ICO、PNG 和 ttk 控件，并正常退出。该检查不注册热键、不修改剪贴板和用户设置。
- 通过 computer-use 打开完整 GUI 的额外检查遇到应用批准超时，未完成可见窗口的人工验收；以上启动自检已实际执行 EXE。

## 使用边界

Edit / RichEdit 的已适配编辑快捷键使用控件命令，不切换前台，也不还原目标窗口。不同记事本版本的编辑控件结构可能不同，多编辑区需手动选择接收窗口。其他程序的消息按键不等价于物理键盘状态，不能保证所有游戏组合键可用。
